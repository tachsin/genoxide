[package]
name = "ga_bench_radiate"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# default features only; no "rayon"/parallel executor: every engine runs single-threaded
radiate = "=1.3.1"

[profile.release]
codegen-units = 1
lto = "thin"
